# Ignora la carpeta de node_modules
node_modules

# Ignora los archivos de log
*.log

# Ignora el directorio de build (no necesario en el contenedor, ya que se compila dentro del Dockerfile)
dist

# Ignora archivos específicos de desarrollo
*.md
.git
.gitignore
.vscode
Dockerfile
docker-compose.yml
npm-debug.log
